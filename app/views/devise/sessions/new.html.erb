
<div class="container">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), :html => {:class => "form-signin"}) do |f| %>
    <div class="text-center mb-4">
      <img class="mb-4" src="/docs/4.3/assets/brand/bootstrap-solid.svg" alt="" width="72" height="72">
      <h1 class="h3 mb-3 font-weight-normal">My annotations</h1>
      <p>Let's create an Evernote-like application with Ruby on Rails. Every day we will have updates on the progress of the project. Let's go<p>
    </div>

    <div class="form-label-group">
      <label for="inputEmail">Email address</label>
      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => "form-control" %>
    </div>

    <div class="form-label-group">
      <label for="inputPassword">Password</label>
      <%= f.label :password %><br />
      <%= f.password_field :password, autocomplete: "Password", :class => "form-control"  %>
    </div>

    <div class="checkbox mb-3">

      <% if devise_mapping.rememberable? %>
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      <% end %>
    </div>

    <%= f.submit "Sign in", :class => "btn btn-lg btn-primary btn-block"  %>
    <p class="mt-5 mb-3 text-muted text-center">&copy; 2019</p>
  <% end %>
<%= render "devise/shared/links" %>
</div>


